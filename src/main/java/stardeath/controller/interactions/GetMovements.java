package stardeath.controller.interactions;

/**
 * An interface representing a way for the interface to get the player's movement choice
 */
public interface GetMovements {

  Movement requestMovement();
}
