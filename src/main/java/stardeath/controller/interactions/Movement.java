package stardeath.controller.interactions;

/**
 * A enum representing all the types of Movement that the player can make
 */
public enum Movement {
  UP, DOWN, RIGHT, LEFT,
  FIRE, GRENADE, INTERACT,
}
